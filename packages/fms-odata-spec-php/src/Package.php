<?php

declare(strict_types=1);

namespace FmsOData\Spec;

/**
 * Package metadata for fms-odata-spec-php.
 *
 * Composer versions come from VCS tags, so the version is not declared in
 * composer.json. This constant exposes the current specification/API version
 * for runtime inspection.
 */
final class Package
{
    /** The specification/API version this package mirrors. */
    public const VERSION = '2.0.1';
}
